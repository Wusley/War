module.exports = ( function() {

  var checkInitiative = function( atks, defs ) {

    var atksId = 0,
        atksLength = atks.length;
    for( ; atksId < atksLength ; atksId = atksId + 1 ) {




    }

    var defsId = 0,
        defsLength = defs.length;
    for( ; defsId < defsLength ; defsId = defsId + 1 ) {


    }

  }

  return checkInitiative;

} () );
